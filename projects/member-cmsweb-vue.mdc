---
description: member-cmsweb-vue 專案規範
globs:
alwaysApply: true
---
此規範適用於 `member-cmsweb-vue`，內容依現有 `README.md`、`CLAUDE.md` 與 docs 文件整理。

## 專案背景

- 此專案為 `Vue 3 + TypeScript + Ant Design Vue 4` 的 CMS 類前端。
- 採用 `Pinia`、`Vue Router`、`Vue I18n`、`Vite`、`pnpm`。
- API 由 Orval / OpenAPI 生成，並透過 API Adapter 統一 response 格式。

## 開發規範

- 你 MUST 遵守 `config-driven` 與 `component reuse` 的既有模式。
- 你 MUST 優先使用 `CusList`、`CusCreateEdit`、`CusPreview` 與既有表單元件庫。
- 你 MUST 使用 `script setup` 與 TypeScript，避免退回 Options API。
- 你 MUST NOT 直接修改 `src/api/generated/` 下的自動生成檔。
- 你 MUST 優先使用 `createApiConfig`、`api-config-helper`、`api-adapter` 等既有機制。

## 文件與流程依據

- 若涉及部署、版本控制、UAT，自動化流程，請優先參考 `documents/GIT_AND_DEPLOY.md` 與 README 中的說明。
- 若專案有 `docs/workflow-guide.md`，你 MUST 以該文件流程為優先依據。
- commit 格式可使用以下任一規範，且不得加入工具標記：
  - `feat #<編號>: <說明>`
  - `[PROJ-123] feat: <說明>`

## Deploy 規範

- 若專案文件明確定義 `UAT` 自動部署 workflow，方可視為自動化目標。
- `PROD` MUST 保持人工核准與人工部署。
- 若變更 `.github/workflows/`、build 指令、API generation config、環境變數，你 MUST 視為 release-sensitive。
- 若環境名稱或實際部署分支與文件不一致，你 MUST 先提出差異，不得自行決定。
